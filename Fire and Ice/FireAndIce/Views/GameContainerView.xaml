<UserControl x:Class="FireAndIce.Views.GameContainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xna="clr-namespace:XNAControl;assembly=XNAControl">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
            <!--<ContentControl x:Name="GameMenu"  
                            VerticalAlignment="Stretch" 
                            HorizontalAlignment="Stretch"
                            Grid.Column="0"/>-->
        <Button VerticalAlignment="Center" x:Name="ReturnToMainMenu">Quit</Button>
        <xna:XNAUserControl x:Name="XNAControl"
                            Grid.Column="1"
                            Height="600"
                            Width="800"/>
    </Grid>
    
</UserControl>
