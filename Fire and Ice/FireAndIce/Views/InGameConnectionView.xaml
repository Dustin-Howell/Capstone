<UserControl x:Class="FireAndIce.Views.InGameConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Visibility="{Binding ThisWindow}">
    <Grid HorizontalAlignment="Center"
          VerticalAlignment="Center"
          Background="{Binding Background}">
        <StackPanel Orientation="Vertical">
            <Label x:Name="ConnectionIssueMessage" HorizontalAlignment="Center" FontSize="20"></Label>
                <Label x:Name="CurrentTime" HorizontalAlignment="Center" Visibility="{Binding CountDownVisible}" FontSize="20"></Label>
            <Button x:Name="ExitButtonActivate" HorizontalAlignment="Center" Visibility="{Binding ExitButton}" Width="120" >Close</Button>
        </StackPanel>
    </Grid>
</UserControl>
